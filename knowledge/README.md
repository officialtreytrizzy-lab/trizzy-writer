# Trey AI Private Knowledge Vault

The app reads private JSON knowledge records from `knowledge/private` or `TREY_KNOWLEDGE_DIR`.

`knowledge/private` is excluded from Git. Store unreleased lyrics, repository summaries, releases, contracts and memory only on trusted storage. Every record needs an ID, title, source and text. Add an updated date and specialty whenever possible.
